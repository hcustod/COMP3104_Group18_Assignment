language: minimal
os: linux
dist: jammy

script:
  - printf 'Travis CI build triggered.\n'
  - printf 'Branch=%s\n' "$TRAVIS_BRANCH"
  - printf 'Repository=%s\n' "$TRAVIS_REPO_SLUG"
  - printf 'Job=%s\n' "$TRAVIS_JOB_NUMBER"
  - printf 'Listing files\n'
  - ls -la
  - printf 'Build finished\n'
  
